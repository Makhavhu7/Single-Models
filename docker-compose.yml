version: '3.8'
services:
  unified-ai:
    image: dorfnew/unified-ai-suite:latest
    ports:
      - "8080:8080"  # Changed to 8080 to match Runpod and FastAPI
    restart: unless-stopped
    environment:
      - HF_TOKEN=${HF_TOKEN}  # Ensure token is passedd